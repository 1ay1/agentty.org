node_modules
.next
out
.DS_Store
*.log
.env*.local
analytics/.env
next-env.d.ts

# automation: deploy state + secrets (never committed)
.last-deployed-tag
deploy/.env

# live backend deps (native, rebuilt on deploy)
deploy/live/node_modules/
