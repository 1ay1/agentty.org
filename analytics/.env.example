# Umami / Postgres secrets for the analytics stack.
# This file is git-ignored — never commit real secrets.
# Copy to .env and fill in:  cp .env.example .env

POSTGRES_PASSWORD=changeme-strong-db-password
UMAMI_APP_SECRET=changeme-long-random-secret
